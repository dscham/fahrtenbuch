import React, {Component} from "react";
import {Typography} from "@rmwc/typography";

export default class Drives extends Component {
    render() {
        return(
            <Typography use="headline1">Drives</Typography>
        );
    }
}